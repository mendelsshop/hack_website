#include <stdio.h>

typedef char * string;
int main(int argc, string argv[])]
{
    if (argc != 2)
    {
        printf("Usage: %s <website-url>\n", argv[0]);
        return 1;
    }
    return 0;
}

